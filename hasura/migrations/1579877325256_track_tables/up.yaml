- type: track_table
  version: 2
  args:
    table:
      schema: public
      name: User
    configuration:
      custom_root_fields:
        select: users
        select_by_pk: user
        select_aggregate: userAggregate
        insert: addUsers
        update: updateUsers
        delete: deleteUsers
